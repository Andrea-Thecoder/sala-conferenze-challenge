version: "3.8"

services:
  postgres:
    image: postgres:18
    container_name: sala-backend-challenge-postgres
    environment:
      POSTGRES_DB: sala-backend-challenge
      POSTGRES_USER: sala-backend-challenge
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - sala-backend-challenge-data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sala-backend-challenge"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sala-backend-challenge-net

  redis:
    image: redis:7-alpine
    container_name: sala-backend-challenge-redis
    ports:
      - "6379:6379"
    volumes:
      - sala-backend-challenge-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sala-backend-challenge-net

volumes:
  sala-backend-challenge-data:
  sala-backend-challenge-redis-data:

networks:
  sala-backend-challenge-net:
